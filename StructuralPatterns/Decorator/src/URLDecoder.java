public class URLDecoder implements Decoder{
    @Override
    public String decode(String data) {
        // Actual decoding logic for URL encoding
        return "Decoded from URL: " + data;
    }
}

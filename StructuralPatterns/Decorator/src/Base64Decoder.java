public class Base64Decoder implements Decoder{
    @Override
    public String decode(String data) {
        // Actual decoding logic for Base64
        return "Decoded from Base64: " + data;
    }
}
